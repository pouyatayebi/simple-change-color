# Simple App to testReact Context Api

This simple app aplling two useContext and useState to change colors of theme
To start, just clone this repo and then enter the following command:

`yarn`

or

`npm i`

